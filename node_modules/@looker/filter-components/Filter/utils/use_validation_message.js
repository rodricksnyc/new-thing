import { useTranslation } from '../../utils';
import { ERROR_TYPE } from '../../constants';
export var useValidationMessage = function useValidationMessage(filterExpression, isRequired) {
  var _useTranslation = useTranslation('use_validation_message'),
      t = _useTranslation.t;

  return isRequired && !filterExpression ? {
    type: ERROR_TYPE,
    message: t('Value required')
  } : {};
};
//# sourceMappingURL=use_validation_message.js.map