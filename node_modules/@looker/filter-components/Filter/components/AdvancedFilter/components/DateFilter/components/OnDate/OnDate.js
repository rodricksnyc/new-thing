import "core-js/modules/es.date.to-string.js";
import "core-js/modules/es.date.now.js";
import { clearTimeFilterDateTimeModel, dateToFilterDateTimeModel, filterDateTimeModelToDate } from '@looker/filter-expressions';
import React from 'react';
import { DateInput } from '../DateInput';
export var OnDate = function OnDate(_ref) {
  var item = _ref.item,
      onChange = _ref.onChange;
  var id = item.id,
      date = item.date;

  var dateChange = function dateChange(newValue) {
    var newDateTimeModel = dateToFilterDateTimeModel(newValue);
    onChange(id, {
      date: clearTimeFilterDateTimeModel(newDateTimeModel)
    });
  };

  var actualDate = date ? filterDateTimeModelToDate(date) : new Date(Date.now());
  return React.createElement(DateInput, {
    date: actualDate,
    onChange: dateChange,
    placement: "right"
  });
};
//# sourceMappingURL=OnDate.js.map