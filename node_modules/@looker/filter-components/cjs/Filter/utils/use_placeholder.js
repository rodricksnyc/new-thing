"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.usePlaceholder = void 0;

var _utils = require("../../utils");

const usePlaceholder = (value, validationMessage) => {
  const _useTranslation = (0, _utils.useTranslation)('use_placeholder'),
        t = _useTranslation.t;

  const anyValue = t('any value');
  const placeholder = !value || value.length === 0 ? (validationMessage === null || validationMessage === void 0 ? void 0 : validationMessage.message) || anyValue : '';
  return {
    'aria-label': anyValue,
    placeholder
  };
};

exports.usePlaceholder = usePlaceholder;
//# sourceMappingURL=use_placeholder.js.map