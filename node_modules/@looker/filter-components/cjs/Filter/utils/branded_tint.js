"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getBrandedTint = void 0;

var _polished = require("polished");

const getBrandedTint = (type, color) => {
  const hue = (0, _polished.parseToHsl)(color).hue;

  switch (type) {
    case 'selected':
      return `hsla(${hue}, 100%, 98%, 100%)`;

    case 'hover':
      return `hsla(${hue}, 25%, 97%, 70%)`;

    case 'pressed':
      return `hsla(${hue}, 50%, 96%, 90%)`;

    case 'border':
      return `hsla(${hue}, 25%, 90%, 100%)`;

    default:
      return `hsla(${hue}, 100%, 98%, 100%)`;
  }
};

exports.getBrandedTint = getBrandedTint;
//# sourceMappingURL=branded_tint.js.map